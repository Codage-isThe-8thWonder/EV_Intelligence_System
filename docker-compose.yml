services:

  backend:
    build: ./Backend
    container_name: ev-backend
    ports:
      - "8000:8000"
    env_file:
      - ./Backend/.env

  frontend:
    build: ./Frontend
    container_name: ev-frontend
    ports:
      - "8501:8501"
    depends_on:
      - backend